# GitHub Actions CD secrets example

# Required: Base64-encoded kubeconfig. The CD workflow will decode this and write $HOME/.kube/config.
# Linux/macOS:
#   base64 -w 0 ~/.kube/config
#   # if -w is not supported (macOS), use: base64 ~/.kube/config | tr -d "\n"
# Windows PowerShell:
#   [Convert]::ToBase64String([IO.File]::ReadAllBytes("$HOME\.kube\config"))
KUBE_CONFIG=

# Optional: Kubernetes namespace to deploy into (the workflow defaults to 'metro').
# NAMESPACE=metro

# Optional: Helm chart path (the workflow defaults to 'deploy/helm/metro').
# CHART_PATH=deploy/helm/metro


