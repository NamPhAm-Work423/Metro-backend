/**
 * @swagger
 * /v1/transport/routes:
 *   get:
 *     summary: Get all routes
 *     tags: [Public Transport]
 *     responses:
 *       200:
 *         description: List of routes
 */
// Add similar for all endpoints from public-service README 